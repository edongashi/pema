{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run tsc",
    "publish": "lerna run tsc && lerna publish"
  },
  "devDependencies": {
    "@types/node": "^12.6.0",
    "lerna": "^3.15.0",
    "react": "^16.8.6",
    "typescript": "^3.5.2"
  }
}
