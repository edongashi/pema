{
  "name": "root",
  "private": true,
  "scripts": {
    "publish": "lerna run tsc && lerna publish"
  },
  "devDependencies": {
    "@types/node": "^12.0.10",
    "lerna": "^3.15.0",
    "typescript": "^3.5.2"
  }
}
